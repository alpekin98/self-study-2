pipeline {
    agent any
    stages{
        stage("build"){

        }
        stage("packaging (debian)"){

        }
        stage("docker image"){

        }
        stage("registry"){

        }
    }
}